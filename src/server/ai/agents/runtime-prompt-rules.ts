export function browserCodeRules() {
  return [
    '- browserCode is the primary browser inspection and operation tool. Put one ordinary JavaScript cell in params.code. Code mode has no UID-click tool.',
    '- The code receives the real Playwright page and context objects. Use ordinary Playwright APIs directly.',
    '- At the beginning of every new or resumed user request, before any browser-changing code, run one separate read-only browserCode cell that returns await browser.user.openTabs(), page.url(), await page.title(), and enough current evidence chosen by the model through page.domSnapshot() or targeted Playwright/DOM reads. openTabs includes active-tab and tab-group metadata. Reuse the correct existing session group and tab; never assume that the previously active page, URL, or mounted UI is still current.',
    '- The JavaScript kernel persists for the browser session. Write top-level statements and top-level await; do not wrap the code in a function or module.',
    '- Use top-level var for reusable bindings or choose fresh names because bindings persist across calls. Emit the result with nodeRepl.write(<JSON-serializable value>).',
    '- browserCode has an infrastructure watchdog that restarts an unresponsive JavaScript kernel. Keep each cell bounded. Playwright locator/action operations default to 5000ms and navigation defaults to 30000ms, so a missing target returns control without destroying persistent bindings. Use an explicit per-operation timeout only when the page has a known longer transition.',
    '- page.domSnapshot() is an explicit read API that returns the Playwright AX tree rooted at the current page body. It is never generated automatically after an operation. The model may instead write targeted Playwright locator, DOM, value, text, URL, or network reads and return only the evidence it needs.',
    '- After an operation, navigation, or tab change, browserCode automatically returns actual.finalPage, actual.domChanges, and code/page console deltas, but never an automatic axTree. domChanges is the direct incremental DOM journal caused by that cell: added/updated/removed contain rendered actionable records, extra contains mounted non-actionable records plus errors and validationErrors, and overflow reports an incomplete delta. It is not a full-page snapshot. Pure successful read cells add no automatic DOM content.',
    '- Treat exact current tags, attributes, hierarchy, editability, and mounted structure as authoritative. Never translate, invent, or replace returned attributes with a guessed semantic locator, and never infer a control type, interaction sequence, or completion state from its label, appearance, or prior experience. Identify a stable parent by data-testid, stable data-*, unique exact href/name, or another exact returned attribute. Page and Locator factory methods automatically exclude matches that do not currently exist as rendered elements, including targets hidden by themselves or an ancestor and targets without a non-empty rendered rectangle; count(), first(), last(), and nth() therefore operate on the rendered set. Every Playwright Locator/Page element action then independently runs full actionability checks on every remaining candidate and executes only when exactly one candidate passes; hidden file inputs used by setInputFiles are the sole rendered-existence exception at the action boundary. Pointer checks use the target final computed style, real hit testing, and an action-specific Playwright trial on every remaining candidate instead of rejecting a target merely because an ancestor has pointer-events:none. Full checks also reject opacity-hidden, aria-hidden, inert, disabled, covered, detached, readonly/non-editable, or otherwise invalid targets. first(), last(), and nth() are allowed when the model intentionally chooses a positional candidate; runtime validates that selected locator normally.',
    '- A cell may perform multiple bounded state-changing operations. Keep their dependencies explicit and use targeted Playwright reads between dependent operations when an earlier result can change later target assumptions. After the cell, inspect its direct result and incremental domChanges. If more state is needed, explicitly call page.domSnapshot() or write a targeted read-only Playwright/DOM check in the next cell. Do not treat Playwright delivery alone as proof of success.',
    '- For pixel evidence, stay inside browserCode: const image = await page.screenshot({ fullPage: false }); await nodeRepl.emitImage(image). Full-page images are read-only; use viewport coordinates only from a freshly emitted viewport image.',
    '- The code runtime also exposes browser/tab: browser.tabs.list()/new()/finalize(), browser.user.openTabs()/claimTab(), tab.playwright, and tab.cua. browser.user.openTabs() returns tab id, active state, URL, title, groupId, and groupTitle. These are JavaScript APIs inside the same browserCode tool.',
    '- DOM code belongs inside page.evaluate. The surrounding JavaScript module runs in an isolated Node process.',
    '- Do not import modules or access Node globals, local files, environment variables, cookies, browser storage, or raw credential values. When the prompt supplies a credential reference, use only await credentialVault.fill(locator, ref); never read the filled field value or write credentials/references to outputs or logs.',
  ];
}

export function browserChatCodeRules(screenshotAvailable = true) {
  return [
    '- Use browserCode for live inspection and operation. It receives the real Playwright page/context and a persistent top-level-await JavaScript kernel: keep cells bounded, use top-level var or fresh names, and return compact evidence with nodeRepl.write(...).',
    '- Before the first browser-changing cell of every new or resumed user request, use a separate read-only cell to return browser.user.openTabs(), page.url(), page.title(), and enough current AX/DOM/Playwright evidence chosen by the model. Confirm the active tab, its group, current page, and relevant rendered state before operating; never act directly from old conversation state.',
    '- Operation/navigation/tab-change results include actual.finalPage, direct incremental domChanges in actual.domChanges, and code/page console deltas, but never an automatic axTree. domChanges added/updated/removed describe only changes caused by that cell; extra carries mounted non-actionable changes and diagnostics; it is not a full snapshot. Explicitly call page.domSnapshot() or write targeted Playwright/DOM reads when more page evidence is needed. Keep DOM-only code inside page.evaluate, and do not access Node globals, files, environment variables, cookies, or browser storage.',
    '- Use exact tags, attributes, hierarchy, editability, and mounted structure from the latest explicit read or domChanges to decide locators; never translate or invent a selector and never infer a control type or interaction sequence from labels, appearance, or prior experience. Multiple bounded operations may run in the same cell, but use targeted Playwright reads before a dependent operation when an earlier action can change its target assumptions. For credentialVault.fill and every Playwright Locator/Page element action, choose a stable parent by data-testid, stable data-*, unique exact href/name, or another returned attribute. Page/Locator factories automatically remove hidden and zero-rectangle matches before count() or positional selection. Runtime then applies target-style and hit-test checks followed by an action-specific Playwright trial to every remaining candidate; it operates only when exactly one candidate passes all stages. An ancestor pointer-events:none alone does not reject a target. first(), last(), and nth() are allowed and their selected locator still receives the same runtime validation.',
    '- If no normal locator uniquely identifies the rendered target, inspect fresh DOM evidence and refine the locator scope. Code mode has no UID-click tool.',
    '- Never use force:true. After timeout, strict-mode failure, or actionability rejection, do not blindly retry the same operation; inspect incremental domChanges or explicitly run page.domSnapshot()/targeted Playwright reads and rebuild the locator. Autonomously choose the read-only checks needed to establish the requested business result. Do not bypass normal element actionability with CUA, page.mouse, DOM click, dispatchEvent, or script click.',
    screenshotAvailable
      ? '- For a Playwright-indescribable visual control only, emit a fresh viewport image with nodeRepl.emitImage(await page.screenshot({fullPage:false})) and end the cell; after the model sees that image, one coordinate/CUA action may be performed in the next cell. Full-page images are read-only, and navigation, viewport change, or DOM redraw invalidates the image.'
      : '- Image and coordinate targeting are unavailable; use Playwright locators or DOM evidence.',
    '- For tabs/windows, use context.pages() or the available browser.tabs/browser.user/tab.playwright/tab.cua APIs. browser.user.openTabs() exposes active and group metadata for continuation. When given a credential reference, use credentialVault.fill(locator, ref) only; never read or output credential values or references.',
  ];
}

export function browserChatDomRules(screenshotAvailable = true) {
  return [
    '- Use inspect action="capture" mode="full" for the complete loaded semantic DOM. Later DOM interactions use only an exact dom-* ref exposed by this most recent snapshot. mode="text" is the reading view of all loaded text; mode="changes" contains only inter-action changes.',
    '- Every change follows OBSERVE -> ONE ACTION -> RE-OBSERVE -> VERIFY BUSINESS STATE. Confirm from a fresh inspect result the current page, [page-state].activeSurface, expected result, exact target, and loading/blocking state. Background DOM nodes may remain listed while a blocking activeSurface owns the current operation scope. If anything is uncertain or the interface may have changed, inspect again instead of acting.',
    '- Continue a paged frozen capture only with its exact nextCursor and the same mode. Never scroll for snapshot pagination. Use inspect action="search" to narrow the current baseline and action="httpRequests" for network evidence.',
    '- Every DOM interaction target contains only kind="ref" and the exact dom-* ref copied from the latest inspect result. Do not send role, name, attributes, scope, target evidence, or a ref from conversation history. The backend checks current snapshot ownership only and does not compare element text, attributes, or a semantic fingerprint. One interact call performs one operation. Its runtime verification result is a hard condition: verification.status="failed" means re-observe and continue from the actual current state, never claim completion or blindly repeat the operation.',
    '- Ordinary clicks still use Playwright actionability and covering-layer checks. Only when a fresh inspect proves the exact intended action is to close the currently visible overlay may you send action="click" with force=true. Force skips only actionability/cover checks; current snapshot ownership remains mandatory. Never use force to make an uncertain target succeed; inspect again immediately afterward.',
    screenshotAvailable
      ? '- No screenshot is attached automatically. When pixel evidence is necessary, call takeScreenshot with capture="viewport" and end that model step; after inspecting the returned image, use interact with either coordinates from that latest viewport screenshot or one current dom-* ref target, never both. Full-page and older screenshots are read-only evidence.'
      : '- Use interact with one current dom-* ref target; coordinate targeting is unavailable.',
    '- interact supports click, hover, drag, scrolling, type, press, shortcuts, and selectOption. DOM targets scroll into view as needed; scroll the page only for confirmed lazy or virtual content.',
    '- For a native select, use interact action="selectOption" with its current dom-* ref and an exact option value or full label. For a container marked virtualized="possible", use selectOption with that container ref and an exact option value or full label; the backend scans the virtual list and clicks the unique mounted match. Do not manually page through a virtual list when this exact selection operation applies.',
    '- Use browser for navigation, waiting, listing tabs, and switching tabs. After an action may open a tab, list tabs before choosing the next target.',
    '- For a supplied credential reference, use interact action="type" with the current field dom-* ref only. Never place the secret in text, read it back, or expose the reference.',
  ];
}

export function browserActionRules(screenshotAvailable = true) {
  return [
    '- Before a bounded sequence of clicks, hovers, fills, presses, selections, drags, credential fills, or other state-changing element actions, use exact evidence that the model already inspected in a preceding explicit page.domSnapshot()/targeted read or in direct incremental domChanges. Multiple operations may run in one cell; use targeted Playwright reads before a dependent operation when an earlier action can change target assumptions.',
    '- Use exact current tags, attributes, hierarchy, editability, and mounted structure with targeted Playwright or read-only DOM inspection. Never translate or invent labels/selectors, and never infer control type, interaction sequence, or completion state from a label, appearance, or prior experience. Identify a stable parent using data-testid, stable data-*, a unique exact href/name, or another exact returned attribute. Page/Locator factories remove hidden and zero-rectangle matches from the rendered set before count() or positional selection. Every action then applies full actionability to the remaining candidates and proceeds only when exactly one candidate passes; otherwise narrow the locator.',
    '- If count() > 1, prefer a stronger stable attribute or parent scope, but first(), last(), and nth() are permitted when a positional choice is intentional. The resulting locator still passes normal visibility and actionability validation. If count() === 0 or current evidence does not identify the target, obtain fresh explicit evidence and rebuild the locator.',
    '- Playwright force: true is forbidden. Runtime actionability rejects a target when it or any ancestor is display:none, visibility-hidden/collapsed, opacity-hidden, aria-hidden, inert, disabled, detached, covered, or has no rendered rectangle. For pointer actions it checks the target final computed pointer-events value, performs a real hit test, and runs an action-specific Playwright trial for every visible candidate; an ancestor pointer-events:none alone is not a rejection. On timeout, ambiguity, or rejection, do not retry or fall back to CUA, page.mouse, DOM element.click(), dispatchEvent(), or script click; inspect current state and construct a tighter locator.',
    '- CUA or coordinate clicking is allowed only for a special visual control that Playwright cannot describe. Use two separate model steps: first end a browserCode cell after emitting a fresh viewport screenshot, then inspect the returned image, confirm the intended point is not obscured, and perform one coordinate click in the next browserCode cell. Same-cell screenshot-and-click is forbidden, and the runtime rejects stale evidence after navigation, viewport change, or DOM redraw.',
    '- Use stable Playwright locators for semantic elements. For native HTML <select>, call locator.selectOption(...) directly.',
    '- A browserCode cell may contain bounded read-only inspection and multiple bounded state-changing operations.',
    '- After the operation sequence inspect its direct result and incremental domChanges. When they are insufficient, explicitly write a read-only page.domSnapshot(), URL, DOM, value, text, or network check. Playwright delivery alone does not prove business success, but no specific verification helper is mandatory.',
    '- Use scrolling only for lazy-loaded, virtualized, or viewport-created content.',
    screenshotAvailable
      ? '- Use page.mouse coordinates only in the browserCode cell after the model has received and inspected a fresh viewport screenshot from the previous cell.'
      : '- Image-coordinate targeting is unavailable; use locators or DOM inspection.',
  ];
}

export function browserContextLine() {
  return 'No page state is preloaded. On every new or resumed browser request, first use a read-only browserCode cell to inspect existing tabs/groups, the active page identity, and enough current AX/DOM/Playwright evidence chosen by the model before changing the browser.';
}

export function screenshotObservationRule(screenshotAvailable = true) {
  return screenshotAvailable
    ? '- Final page identity and console deltas are attached automatically. Actual browser operations, navigation, and tab changes also attach direct incremental domChanges, but never an automatic axTree; pure successful reads attach no automatic DOM content. No screenshot is attached automatically. When AX, DOM, or pixel evidence is necessary, write the corresponding read explicitly in browserCode; emit pixels with await nodeRepl.emitImage(await page.screenshot({ fullPage: false })) so the image becomes visible to the model in the next model step.'
    : '- No image observation is available. Use browserCode for inspection and browser operations.';
}
