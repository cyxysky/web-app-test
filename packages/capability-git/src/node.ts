import { spawn } from 'node:child_process'; import path from 'node:path'; import { createGitCapability, type GitOperations } from './index.js'; import type { CapabilityExecutionContext, CapabilityRunContext } from '@webpilot/capability-sdk';
function gitCommand(repository: string, args: string[], context: CapabilityExecutionContext, maximum: number, timeoutMs: number, stdin?: string) { return new Promise<{ stdout: string; stderr: string }>((resolve, reject) => { const trustedRepository = repository.replace(/\\/g, '/'); const child = spawn('git', ['-c', `safe.directory=${trustedRepository}`, '-C', repository, ...args], { shell: false, windowsHide: true, stdio: 'pipe' }); let stdout = ''; let stderr = ''; child.stdout.on('data', (chunk) => { stdout = (stdout + String(chunk)).slice(0, maximum); }); child.stderr.on('data', (chunk) => { stderr = (stderr + String(chunk)).slice(0, maximum); }); child.stdin.end(stdin); const stop = () => child.kill('SIGKILL'); const timer = setTimeout(stop, timeoutMs); timer.unref?.(); const onAbort = () => stop(); context.abortSignal?.addEventListener('abort', onAbort, { once: true }); child.once('error', (error) => { clearTimeout(timer); context.abortSignal?.removeEventListener('abort', onAbort); reject(error); }); child.once('close', (code) => { clearTimeout(timer); context.abortSignal?.removeEventListener('abort', onAbort); if (code === 0) resolve({ stdout, stderr }); else reject(new Error(stderr || `Git exited with code ${code}.`)); }); }); }
function safePaths(paths: string[] | undefined) { return (paths || []).map((value) => { if (path.isAbsolute(value) || value.split(/[\\/]/).includes('..')) throw new Error(`Git path must stay inside the repository: ${value}.`); return value; }); }
export function createNodeGitOperations(input: { repository: string }): GitOperations { const repository = path.resolve(input.repository); return { async run(request, context) { const paths = safePaths(request.paths); let args: string[]; let stdin: string | undefined; if (request.action === 'status') args = ['status', '--short', '--branch']; else if (request.action === 'branches') args = ['branch', '--all', '--no-color']; else if (request.action === 'log') args = ['log', '--oneline', '--decorate', '-n', '50', ...(request.ref ? [request.ref] : [])]; else if (request.action === 'show') args = ['show', '--stat', '--format=fuller', request.ref || 'HEAD', ...(paths.length ? ['--', ...paths] : [])]; else if (request.action === 'diff') args = ['diff', '--no-ext-diff', '--no-color', ...(request.ref ? [request.ref] : []), ...(paths.length ? ['--', ...paths] : [])]; else if (request.action === 'applyPatch') { args = ['apply', '--whitespace=nowarn', '-']; stdin = request.patch; } else { await gitCommand(repository, ['add', '--', ...paths], context, request.maxOutputChars, request.timeoutMs); args = ['commit', '-m', request.message!, '--', ...paths]; } const output = await gitCommand(repository, args, context, request.maxOutputChars, request.timeoutMs, stdin); return { repository, ...output }; }, async health() { try { await gitCommand(repository, ['rev-parse', '--is-inside-work-tree'], { invocationId: 'health' }, 1000, 10_000); return { status: 'healthy' }; } catch (error) { return { status: 'unhealthy', message: error instanceof Error ? error.message : String(error) }; } } }; }
export function createNodeGitCapability(input: { repository: string | ((context: CapabilityRunContext) => string) }) { return createGitCapability({ createOperations(context) { return createNodeGitOperations({ repository: typeof input.repository === 'function' ? input.repository(context) : input.repository }); } }); }
