import type { CapabilitySkill } from '@webpilot/capability-sdk';
export const gitRuntimeSkillId = 'system-git-runtime';
export const gitRuntimeSkill = Object.freeze({ id: gitRuntimeSkillId, title: 'Git Runtime', summary: `<system_skill id="${gitRuntimeSkillId}">Inspect repository status and diffs before mutations; preserve unrelated work and never rewrite history.</system_skill>`, content: `# Git Runtime\n\n- Read status and the relevant diff before applying a patch or committing.\n- Existing changes belong to the user. Preserve unrelated files and overlapping edits.\n- Never rewrite history, force push, hard reset, or discard work.\n- Apply only a focused standard Git unified diff. Do not use Codex \`*** Begin Patch\` / \`*** End Patch\` markers.\n- For example, create a one-line file with exactly:\n\n\`\`\`diff\ndiff --git a/example.txt b/example.txt\nnew file mode 100644\n--- /dev/null\n+++ b/example.txt\n@@ -0,0 +1 @@\n+example\n\`\`\`\n\n- Git writes require host configuration and approval; a commit must include only the intended paths.`, required: true, activation: [{ toolName: 'git', actions: ['status', 'diff', 'log', 'show', 'branches', 'applyPatch', 'commit'] }] } satisfies CapabilitySkill);
