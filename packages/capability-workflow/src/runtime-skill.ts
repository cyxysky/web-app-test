import type { CapabilitySkill } from '@webpilot/capability-sdk'; export const workflowRuntimeSkillId = 'system-workflow-runtime'; export const workflowRuntimeSkill = Object.freeze({ id: workflowRuntimeSkillId, title: 'Workflow Runtime', summary: `<system_skill id="${workflowRuntimeSkillId}">Use durable workflows only for multi-stage work, respect dependencies, and checkpoint verified outcomes rather than intentions.</system_skill>`, content: `# Workflow Runtime\n\n- Create a workflow only when work has multiple durable stages or must resume later.\n- Give every step a stable id and explicit dependencies; cycles are rejected.\n- Mark a step completed only after its outcome is verified.\n- Record concise outputs and blockers, not hidden reasoning or raw logs.\n- A workflow is complete only when every step is completed; cancelled and failed states remain explicit.`, required: true, activation: [{ toolName: 'workflow', actions: ['create', 'list', 'read', 'updateStep', 'cancel'] }] } satisfies CapabilitySkill);
