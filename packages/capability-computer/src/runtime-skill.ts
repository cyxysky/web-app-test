import type { CapabilitySkill } from '@webpilot/capability-sdk'; export const computerRuntimeSkillId = 'system-computer-runtime'; export const computerRuntimeSkill = Object.freeze({ id: computerRuntimeSkillId, title: 'Computer Runtime', summary: `<system_skill id="${computerRuntimeSkillId}">Observe the current desktop before every dependent input action, use exact current coordinates, and verify the resulting state.</system_skill>`, content: `# Computer Runtime\n\n- Observe or capture a fresh screenshot before acting.\n- Coordinates are valid only for the exact current display, window layout, scale, and screenshot.\n- Use the smallest input action and observe again after every state change.\n- Never type secrets unless the host provides a protected fill primitive.\n- If a locked Windows secure desktop prevents capture, retry once, then ask the host to unlock it; never attempt to bypass authentication.\n- External, destructive, privacy-sensitive, or costly actions require host approval and direct verification.`, required: true, activation: [{ toolName: 'computer', actions: ['observe', 'screenshot', 'click', 'type', 'key', 'scroll', 'wait'] }] } satisfies CapabilitySkill);
