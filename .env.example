OPENAI_API_KEY=-
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/web_app_test?schema=public"
# 无头浏览器
HEADLESS_BROWSER=false
# 浏览器延迟
BROWSER_SLOW_MO_MS=0
# 是否是测试内容
AI_TEST_DEBUG=true
# 最大步数
AI_TEST_RUNTIME_MAX_STEPS=100
# 是否是点击模型 visual-markers dom
AI_BROWSER_MODE=visual-markers
# 图像压缩大小
AI_SCREENSHOT_MAX_KB=20000
AI_PROVIDER=gemini
WEBPILOT_BASE_PATH=/webpilot
BROWSER_PREVIEW_TRANSPORT=video
BROWSER_PREVIEW_FPS=20
BROWSER_PREVIEW_VIDEO_BITRATE_KBPS=
BROWSER_PREVIEW_VIDEO_SOURCE_FORMAT=png
# 2K: 2560x1440; UHD 4K: 3840x2160; DCI 4K: 4096x2160.
BROWSER_PREVIEW_VIDEO_MAX_WIDTH=1920
BROWSER_PREVIEW_VIDEO_MAX_HEIGHT=1080
BROWSER_USER_BROWSER_IDLE_TIMEOUT_MS=600000
# Optional override. The bundled ffmpeg-static binary is used when empty.
FFMPEG_PATH=
# Local direct access uses this ID. Default: 1.
WEBPILOT_DEFAULT_USER_ID=1
# Online mounted deployments should require the ID supplied to WebPilotQA.mount().
WEBPILOT_REQUIRE_MOUNT_USER_ID=false
# Set true only for a cross-site HTTPS iframe mount. This uses SameSite=None; Secure.
WEBPILOT_CROSS_SITE_MOUNT=false
# Optional: 15-120 seconds. Default: 45 seconds.
WEBPILOT_WEBSOCKET_TICKET_TTL_SECONDS=45
# Optional PDF font override for custom images without a bundled CJK font.
WEBPILOT_DOCUMENT_FONT=
WEBPILOT_DOCUMENT_FONT_FAMILY=
# Enable only when WebPilot is reachable exclusively through a trusted reverse proxy.
WEBPILOT_TRUST_PROXY=false
WEBPILOT_ADMIN_SETTINGS_PASSWORD_ENABLED=true
WEBPILOT_ADMIN_SETTINGS_PASSWORD=123456
