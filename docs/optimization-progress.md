# 项目优化演进状态说明

更新时间：2026-06-12

## 具体实现目标

把当前项目从一个“能运行的 AI 浏览器测试工具”演进成一个可长期迭代、可打包交付、具备产品护城河的 AI Web 测试工作台。

核心目标包括：

1. 建立稳定的 `Agent Loop`，让 AI 执行过程不再只依赖短期对话上下文。
2. 引入 `Visual Context Manager`、`Working Memory` 和 trace store，让视觉证据、工具调用、任务进度可以被持续追踪。
3. 建立结构化任务框架、覆盖矩阵、台账、证据图和最终报告，让运行结果能沉淀为可审计资产。
4. 处理长上下文压缩问题，让长任务在上下文接近上限时仍能保留目标、进度和证据。
5. 将运行时数据从 JSON 文件切换为 SQLite 数据库，支持桌面打包和本地持久化。
6. 继续向最终产品形态演进，形成区别于普通浏览器自动化脚本的能力壁垒。

## 当前实现状态

总体判断：工程底座已经完成约 80% 到 85%；最终产品化与护城河建设约完成 40% 到 50%。

| 模块 | 当前完成度 | 状态 | 说明 |
| --- | ---: | --- | --- |
| Agent Loop / 执行循环 | 80% | 已落地 | 已有分模式执行策略、运行时状态、工具调用链路和上下文注入。后续需要更多稳定性评测。 |
| Visual Context Manager | 75% | 已落地 | 已接入视觉上下文管理、引用截图选择和运行时调试记录。后续需要优化视觉引用淘汰策略。 |
| Working Memory / trace store | 80% | 已落地 | 已将执行过程中的任务框架、台账、证据和进度写入运行记录。后续需要更强的跨运行复用。 |
| 长上下文压缩 | 75% | 已落地 | 已有结构化 context summary，能保存目标、进度、台账摘要和证据摘要。后续需要做更系统的压缩质量评测。 |
| taskFrame / ledger | 85% | 已落地 | 已支持任务框架生成、编辑、运行时携带、台账采集和报告使用。 |
| 覆盖矩阵 / 证据图 / 诊断 | 75% | 已落地 | 运行页和报告已能展示覆盖、证据索引、证据关系和诊断指标。后续需要做结果对比和趋势分析。 |
| browser-chat 会话 | 80% | 已落地 | 会话、消息、步骤、日志已迁入 SQLite；仍保留内存中的活跃浏览器对象。 |
| SQLite 运行时存储 | 90% | 已落地 | 用例、运行、分组、设置、模型配置、计划任务、browser-chat 会话已进入 SQLite。 |
| 桌面打包存储准备 | 75% | 基本完成 | Prisma SQLite schema、生成脚本、健康检查和初始化接口已具备。后续要做真实 installer 验证。 |
| 最终产品护城河 | 40% | 进行中 | 技术底座已具备，但还缺评测集、站点知识沉淀、团队工作流、模板市场和可量化稳定性指标。 |

## 已完成内容

### 1. 数据库切换

已完成：

- 新增 SQLite runtime engine。
- `mock-store.ts` 已改为通过 SQLite 读写，文件名仍是历史命名。
- `prisma/schema.prisma` 和 `prisma/desktop/schema.prisma` 已切到 SQLite。
- 新增 `BrowserChatSession` 表。
- browser-chat 会话不再写入 `browser-chat-sessions.json`。
- 删除 JSON store 同步、迁移快照和 atomic JSON 写入逻辑。
- 存储健康面板现在显示 SQLite 数据库状态和记录数量。

未完成：

- 尚未建立正式的 schema migration 版本管理策略。
- 尚未提供数据库备份、恢复、导出、导入能力。
- 尚未做桌面安装包的一轮完整真实机器验证。

### 2. 长上下文与结构化压缩

已完成：

- 增加结构化 context summary。
- 压缩内容包含目标、当前进度、后续行动、台账摘要和证据摘要。
- 长任务不再主要依赖原始历史回放，而是依赖语义状态继续推进。

未完成：

- 还没有自动评估“压缩前后是否丢失关键事实”的测试集。
- 还没有对不同任务长度、截图数量、站点复杂度做系统压测。

### 3. 任务框架、台账和报告

已完成：

- 支持根据用户需求生成 `taskFrame`。
- 支持在用例编辑器中修改任务框架。
- 运行步骤会携带 task frame、ledger items、working memory。
- 最终报告会使用任务框架、台账、证据索引、覆盖矩阵和诊断信息。

未完成：

- 报告还缺多模板能力，例如冒烟测试报告、回归测试报告、竞品分析报告、验收报告。
- 还缺报告质量评分和自动复核机制。

### 4. 运行页与证据展示

已完成：

- 运行页已展示结构化台账。
- 已加入 coverage matrix。
- 已加入 evidence graph。
- 已加入 run diagnostics。
- 已将 report writer 与这些结构化数据打通。

未完成：

- 还缺跨运行对比，例如同一用例多次执行的趋势、失败聚类、 flaky 分析。
- 还缺证据搜索和过滤能力。

### 5. browser-chat 与实时状态

已完成：

- browser-chat 会话持久化进入 SQLite。
- 会话 API 已适配异步数据库读取。
- SSE 初始快照支持异步读取。
- 运行和 browser-chat 的实时状态更新链路已更稳。

未完成：

- browser-chat 还可以进一步沉淀成“可复用探索记录”和“一键生成测试套件”。
- 活跃浏览器对象仍在内存中，这是合理设计，但后续需要更明确的恢复策略。

### 6. 产品化基础

已完成：

- README 已更新为 SQLite-only 存储说明。
- 桌面 Prisma schema 已独立放在 `prisma/desktop`。
- 存储状态 API 和初始化 API 已具备。
- 当前验证已通过 `npm run typecheck`、`npm run prisma:generate`、`npm run prisma:desktop:generate` 和 `git diff --check`。

未完成：

- 还没有完整的首启引导。
- 还没有项目/工作区维度的数据隔离。
- 还没有最终桌面安装包的真实验收记录。
- 还没有稳定的自动化回归测试套件。

## 后续执行方案

### 阶段 A：数据库产品化收口

目标：让 SQLite 不只是能用，而是能安全交付。

建议任务：

1. 重命名 `mock-store.ts` 为更准确的 `runtime-store.ts` 或 `sqlite-store.ts`。
2. 增加数据库备份、恢复、导出、导入。
3. 增加 schema version 表，建立迁移版本记录。
4. 存储健康页加入数据库路径、大小、最近写入、备份状态。
5. 明确 artifacts 与 SQLite 的边界：数据库存结构化数据，截图和报告附件继续走文件系统。

### 阶段 B：稳定性与评测体系

目标：把“看起来能跑”变成“可证明稳定”。

建议任务：

1. 建立一组固定测试站点和任务集。
2. 给 DOM mode、visual markers、browser-chat、replay repair 分别做成功率统计。
3. 建立长上下文压缩评测，检查目标、进度、证据是否保真。
4. 给报告输出建立质量检查，例如是否包含关键证据、是否覆盖所有任务维度、是否引用了错误事实。

### 阶段 C：护城河能力

目标：形成普通自动化脚本难以替代的产品优势。

建议方向：

1. 站点知识库：沉淀每个站点的登录方式、页面结构、常见失败、可靠选择器、业务概念。
2. 测试资产图谱：将用例、运行、步骤、工具、证据、问题、风险连成可查询图谱。
3. 自我改进闭环：失败后自动归因，沉淀 repair 经验，下次执行优先复用。
4. 行业模板：为电商、后台管理、SaaS、内容站、表单流程等场景提供模板化检查维度。
5. 结果对比：支持同一流程在不同时间、环境、账号、版本之间做差异分析。
6. 可解释报告：不仅告诉用户失败了，还能说明证据链、风险影响、复现路径和建议修复优先级。

### 阶段 D：交付与体验

目标：变成用户可以长期使用的桌面产品。

建议任务：

1. 完成桌面安装包真实验证。
2. 增加首启向导，包括模型配置、浏览器配置、数据目录说明。
3. 增加项目空间，用于隔离不同产品、客户或站点。
4. 增加模板用例库和示例运行。
5. 增加失败恢复入口，例如重新执行失败步骤、从某一步继续、导出问题单。

## 对此前的总结

此前最重要的演进不是单个 UI 或单个 API，而是把项目的数据和执行模型从“临时过程记录”推进到了“可沉淀、可解释、可继续”的结构：

- AI 不再只是每一步临时看页面，而是围绕 Agent Loop、Working Memory、Visual Context Manager 和 trace store 执行。
- 长任务不再只依赖原始聊天历史，而是依赖结构化上下文摘要延续。
- 报告不再只是步骤列表，而是能结合任务框架、台账、覆盖、证据和诊断。
- 数据不再依赖本地 JSON 文件，而是统一进入 SQLite，具备桌面打包和长期本地存储基础。

现在项目已经具备“产品底座”，下一阶段的重点应该从继续堆功能，转向验证稳定性、沉淀测试资产、形成站点知识和证据链能力。

## 风险与注意事项

1. 当前数据库中仍会把复杂对象以 JSON 字符串字段存储，这是数据库内部字段设计，不是运行时依赖 JSON 文件。
2. 截图、上传图片、Playwright trace、报告附件仍保留在文件系统中，这是合理的 artifacts 存储方式。
3. 旧的 `.data/store.json` 不再作为运行时来源；如果未来要兼容历史用户数据，需要单独做一次导入工具。
4. `mock-store.ts` 文件名已经不准确，应在后续重命名，避免误导维护者。
5. 目前没有跑 `dev` 或 `build`，符合当前项目要求；已用类型检查和 Prisma generate 验证。

## 下一步优先级

优先级建议：

1. 数据库 schema version、备份恢复、store 文件重命名。
2. 建立最小评测集，验证长任务、browser-chat、DOM mode 和视觉模式。
3. 增加站点知识库和跨运行对比。
4. 做桌面安装包真实验收。
5. 打磨首启体验和项目空间。
